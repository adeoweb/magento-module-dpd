<div id="shipping-additional-info">
    <?php foreach ($block->getAdditionalInfo() as $item): ?>
        <?= $block->escapeHtml($item['label']); ?>: <?= $block->escapeHtml($item['value']); ?>
    <?php endforeach; ?>
</div>
<script>
    require(['jquery'], function ($) {
        $(window).ready(function() {
            $('.shipping-description-title')
            .after($('#shipping-additional-info'));

            $('.order-shipping-method')
            .after($('#shipping-additional-info'));

            $('.order-shipping-method .admin__page-section-item-content')
            .after($('#shipping-additional-info'));
        });
    });
</script>
